import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'vibe.rueberg.eu',
  appName: 'fru-vibe-app',
  webDir: 'dist/frontend/browser'
};

export default config;
